source 'https://rubygems.org'


# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '~> 5.0.0', '>= 5.0.0.1'
# Use postgresql as the database for Active Record
gem 'pg', '~> 0.19.0.pre20160409114042'
# Use Puma as the app server
gem 'puma', '~> 3.0'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.2'
# Turbolinks makes navigating your web application faster. Read more: https://github.com/turbolinks/turbolinks
gem 'turbolinks', '~> 5'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.5'
#Use for scalable asynchronous clients for various protocols, RESTful APIs and so on. Examples: em-http-request
gem 'eventmachine'
#http client for event machine
gem 'em-http-request'
#Use for event machine twitter handeler
gem 'tweetstream'
#Use pry for debugging and console work in code
gem 'pry'
#Daemon for background processes
gem 'daemons'
#Devise for user Auth
gem 'devise'
#Allow for JSON implementations
gem 'json'
# Use bootstrap framework
gem 'bootstrap-sass'
#automatically adds the proper vendor prefixes to your CSS code when it is compiled.
gem 'autoprefixer-rails'
#jquery-rails
gem 'jquery-rails', '~> 4.1', '>= 4.1.1'
#faker data
gem 'faker'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug', platform: :mri
end

group :development do
  # Access an IRB console on exception pages or by using <%= console %> anywhere in the code.
  gem 'web-console'
  gem 'listen', '~> 3.0.5'
  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'
  gem 'spring-watcher-listen', '~> 2.0.0'
  gem "rails_12factor", group: :production
end

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]
